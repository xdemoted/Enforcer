{
  "name": "enforcer",
  "version": "2.0.0",
  "description": "",
  "main": "/build/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node build/index.js",
    "cleanExport": "zip -r build.zip ./build && zip -d build.zip /build/assets && zip -ur build.zip ./package.json",
    "fullExport": "zip -r build.zip ./build && zip -ur build.zip ./package.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.5.1",
    "canvas": "^2.11.2",
    "discord.js": "^14.13.0",
    "quantize": "^1.0.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "@types/quantize": "^1.0.2"
  }
}

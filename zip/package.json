{
  "name": "enforcer",
  "version": "2.0.0",
  "description": "",
  "main": "/build/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node build/main.js",
    "cleanExport": "cp -r ./build ./zip && rm -rf ./zip/build/assets && cp ./package.json ./zip && cp ./token.json ./zip && zip -r build.zip ./zip",
    "fullExport": "cp -r ./build ./zip && cp ./package.json ./zip && cp ./token.json ./zip && zip -r build.zip ./zip"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.5.1",
    "canvas": "^2.11.2",
    "discord.js": "^14.13.0",
    "quantize": "^1.0.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "@types/quantize": "^1.0.2"
  }
}
